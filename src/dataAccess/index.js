const deliveryOrderDb = require("./deliveryOrder.db");
const productDb = require("./product.db");
const userDb = require("./user.db");
const userOrderDb = require("./userOrder.db");

module.exports = Object.freeze({
  deliveryOrderDb,
  productDb,
  userDb,
  userOrderDb,
});
